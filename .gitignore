# src/dist folder is generated by gulp default task,
# hence doesn't need to be on remote repo
src/dist

# project's dependencies are generated by npm install,
# hence doesn't need to be on remote repo
node_modules/